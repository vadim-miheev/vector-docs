apiVersion: v1
kind: ConfigMap
metadata:
  name: vector-docs-config
  namespace: vector-docs
data:
  # Database
  POSTGRES_DB: "vector_docs"
  POSTGRES_PORT: "5432"

  # Embedding service
  EMBEDDING_BASE_URL: "http://ollama:11434/v1"
  EMBEDDING_MODEL_NAME: "nomic-embed-text"
  EMBEDDING_CHUNK_SIZE: "1200"
  EMBEDDING_CHUNK_OVERLAP: "80"

  # Search
  SEARCH_TOP_K: "10"

  # LLM API
  APP_CHAT_BASE_URL: "https://api.deepseek.com/v1"
  APP_CHAT_MODEL_NAME: "deepseek-chat"

  # JWT
  JWT_TTL_SECONDS: "86400"

  # Internal UI URL (frontend service)
  INTERNAL_UI_URL: "http://frontend:80"

  # Mail SMTP
  SPRING_MAIL_HOST: "smtp.gmail.com"
  SPRING_MAIL_PORT: "587"
  SPRING_MAIL_SMTP_AUTH: "true"
  SPRING_MAIL_SMTP_STARTTLS_ENABLE: "true"

  # OCR
  TESSDATA_LANG: "eng"

  # Storage
  SITE_ROOT: "http://localhost:8080"
  STORAGE_SERVICE_HOST: "http://localhost:8080/api/storage"
  DEMO_USER_ID: "1"

  # Kafka
  KAFKA_BOOTSTRAP_SERVERS: "kafka:9092"

  # Notification service host (used by answer-generator)
  APP_NOTIFICATION_HOST: "notification-service"
